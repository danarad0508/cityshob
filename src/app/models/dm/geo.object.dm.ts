export interface GeoObjectDm {
  id: number;
  type: number;
  name1: string;
  name2: string;
  connType: number;
}
//this object is only for example of doing transformation in case data from server is not as we want to use on FE
